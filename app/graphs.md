- compare usage by prompt category, can also be by time unit

- compare usage from week to week > data from this week + data from last week in one graph (or day, month, etc.)

- compare models 

dropdown menu with time(day(data per hour), week(data per day), month(data per week)) and data_type(?)(prompts, model, categories) and water/co2/energy
show current and old (today and yesterday) in one graph
- output by category per day > line graph with all categories
- output of all prompts per time unit
- output per model per time unit


- avg, max, min PER MODEL all together in a graph? or in a table? > select model in dropdown menu
    - prompt length
    - tokens in
    - response length
    - tokens out
    - latency
    - streaming_duration
    - session duration?
    - session prompt count
    - energy, water, co2
- number of times:
    - file attached
    - image attached
    - voice input?
    
 <!-- against number of prompts? > show footprint and show prompt count? > combo bar/line > wont work -->